# Scorekeeper

## Development

Use `mise` to install tools and run common tasks:

```bash
mise install
mise run serve
```

For automatic rebuilds while you edit:

```bash
mise run dev
```

`mise run dev` uses `air` to restart the Go server whenever tracked files change.
